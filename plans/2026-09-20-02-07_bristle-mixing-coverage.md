# Bristle 混色の曲線被覆修正

## Phase 1・2: 承認済みの契約

ユーザー承認: 割れを改善し、表現の変化と性能劣化を修正前後で計測する。
公開API・設定既定値・混色の更新式は維持。混色ONの色用形状でmaskを削らず、coverageは既存maskが決める。
CPUはmaskの画素走査へ色の補間を統合。GPUは一様断面alphaの重複適用と不要なink passを除去。
色の配置が変わる部分は旧描画との画素差として記録し、隠さない。

## Phase 3

- 同一入力のCPU/GPU、混色ON/OFF、空下地と色付き下地、曲線・折返し・筆圧・太径の比較fixtureと性能計測を用意。
- 修正前の結果を保存し、修正後と比較。描画とreadbackを分けて計測する。
- 曲線coverageの回帰テストと既存混色・parity・全テストを実行。

## Phase 4

コードとドキュメントを照合し、劣化・限界を含む計測結果を記録する。先に承認されたリリース前レビュー修正と合わせてコミットする。

## 完了結果

CPUの色補間をmask走査に統合、GPUのink pass/uploadを除去。80条件比較・784テスト・build・lint成功。Phase 4で公開API不変、責務・docs整合を確認。色差・性能・実機未確認の範囲は [検証結果](notes/2026-09-20-bristle-mixing-coverage-results.md) に記録。
