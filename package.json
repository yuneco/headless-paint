{
  "name": "headless-paint",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -F @headless-paint/web dev",
    "build": "pnpm -r build",
    "test": "vitest",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@vitest/browser": "^3.0.0",
    "playwright": "^1.49.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
